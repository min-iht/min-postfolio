# 나중에 나 혼자 볼 설명서

포트폴리오 사이트(https://min-iht.github.io/min-postfolio/)의 글을
혼자서 고치고 올리는 방법. 2026년 7월에 정리해 둠.

---

## 0. 딱 세 가지만 기억하기

1. **글자는 고쳐도 된다. `<`와 `>`로 감싸인 것(태그)은 절대 건드리지 않는다.**
2. 고쳤으면 **저장(Ctrl+S)** → 폴더의 **`업데이트.bat` 더블클릭**. 끝.
3. 뭔가 망가진 것 같으면 당황하지 말고 → 맨 아래 **6번(되돌리기)**. 어떤 버전도 사라지지 않고 전부 보관되어 있다.

---

## 1. 텍스트 수정하는 법

### 파일 열기
- 고칠 파일을 **우클릭 → 연결 프로그램 → Visual Studio Code**(또는 메모장)로 연다.
- 파일은 4개다:
  | 파일 | 내용 |
  |---|---|
  | `min.postfolio-ko.html` | 한국어 본편 |
  | `min.postfolio-en.html` | 영어 본편 |
  | `min.postfolio-jp.html` | 일본어 본편 |
  | `index.html` | 첫 화면(언어 선택) |

### 고칠 곳 찾기
- **Ctrl+F**를 누르고 `수정구역`을 검색하면 이런 주석이 나온다:

```html
<!-- ✏️ 수정구역: Works 2 — TOIL 카드 -->
    ... 여기 사이의 글자만 고친다 ...
<!-- ✏️ 수정구역 끝 -->
```

- 구역 이름으로 바로 검색해도 된다. 예: `수정구역: CV`
- 전체 구역 목록은 아래 **4번**에 있다.

### 고치는 규칙
- 두 주석 **사이**에서, **화면에 보이는 글자만** 고친다.
- `<p class="...">` 같은 태그, `class="..."` 따옴표 안, `src="..."` 경로는 **건드리지 않는다.**
- 예시 — 날짜를 고치고 싶다면:
  - 고치기 전: `<dd>2026.03 - 2026.06</dd>`
  - 고친 후:&nbsp;&nbsp; `<dd>2026.03 - 2026.09</dd>`  ← 숫자만 바뀜, `<dd>`는 그대로
- `<br>`은 "여기서 줄바꿈"이라는 뜻. 이것만은 예외적으로 지우거나 추가해도 된다.
- 글 중간에 `.` 점 하나만 있는 곳(노트 본문의 Problem, Insight 등)은 **아직 글을 안 쓴 자리**다. 점을 지우고 글을 쓰면 화면에 나온다.

### 세 언어 함께 고치기
- 한국어 파일을 고쳤으면 **영어(-en)·일본어(-jp) 파일의 같은 구역도 같이** 고친다.
- 구역 이름이 세 파일에서 **완전히 똑같으니**, 같은 이름으로 검색하면 같은 자리가 나온다.

### 예외 하나: 워크숍 목록 카드
- 워크숍 **목록에 뜨는 카드**(제목/날짜/장소/썸네일)만은 HTML이 아니라
  `js/workshop.js` 파일 맨 위 `WORKSHOPS` 목록에서 고친다.
  파일 안에 한국어 설명 주석이 자세히 달려 있고, 한 곳만 고치면 세 언어에 다 적용된다.
- 사진을 바꾸거나 추가하는 건 이 설명서 범위 밖 — HTML 안의 한국어 주석(슬라이드 데이터 부분)에 방법이 적혀 있고, 새 사진은 1장에 300KB를 넘기지 않게 줄여서 넣는다.

---

## 2. 사이트에 올리는 법 (업데이트 스크립트)

1. 폴더에서 **`업데이트.bat` 더블클릭** (까만 창이 뜬다).
2. 바뀐 파일 목록이 보이고, **"어떤 걸 바꿨는지 한 줄로 적어주세요"**라고 물어본다.
   - 짧게 메모하고 Enter (예: `TOIL 기간 수정`)
   - **그냥 Enter만 치면** 오늘 날짜로 자동 저장된다.
3. `완료! GitHub에 잘 올라갔어요.`가 나오면 성공. **1~2분 뒤** 실제 사이트에서 확인.
   - 사이트가 안 바뀐 것처럼 보이면 **Ctrl+F5** (강력 새로고침).
4. `[문제 발생]`이 뜨면 창의 안내문을 읽고, 아래 **5번**을 본다.

> `업데이트.bat`과 `update_site.ps1`은 한 세트다. 둘 다 지우거나 다른 데로 옮기지 말 것.

---

## 3. 무엇이 어떻게 저장되는 건지 (30초 설명)

- 이 폴더는 **git**이라는 도구가 지켜보고 있다. 업데이트할 때마다 "그 시점의 스냅샷"이 하나씩 쌓인다.
- `업데이트.bat`은 ① 바뀐 파일을 담고(add) ② 스냅샷을 만들고(commit) ③ GitHub에 올린다(push).
- GitHub에 올라가면 사이트(GitHub Pages)가 자동으로 새 버전을 보여준다.
- 스냅샷이 전부 남아 있어서 **언제든 과거로 돌아갈 수 있다.** 그래서 웬만한 실수는 복구된다.

---

## 4. 수정구역 전체 목록

### 본편 3개 파일 공통 (ko / en / jp 모두 같은 이름, 34개)

Ctrl+F로 아래 이름을 그대로 검색하면 된다. 줄 번호는 수정할 때마다 조금씩 밀리니 검색이 정확하다.

**첫 화면(히어로)**
| 구역 이름 | 안에 있는 것 |
|---|---|
| 히어로 — 인트로 인사말 | "안녕하세요! / 당신을 뭐라고 불러드리면 좋을까요?" |
| 히어로 — 엽서 편지 문구 | 봉투에서 나오는 편지 (Dear ~ From 강민경) |

**Works (프로젝트 6개 — 카드마다 분류·제목·설명·Question/Answer·태그·기간·역할·Outcome 수치가 전부 들어 있음)**
| 구역 이름 |
|---|
| Works 1 — 대화의 몸집 프로젝트 카드 |
| Works 2 — TOIL 카드 |
| Works 3 — 보따리(Bottari) 카드 |
| Works 4 — The Mission For You Is 카드 |
| Works 5 — 어린 밤을 지나고 있어 카드 |
| Works 6 — 눈을 덮고 푸른 꿈을 꿔 카드 |

**워크숍·CV·Info**
| 구역 이름 | 안에 있는 것 |
|---|---|
| 워크숍 — 섹션 제목 | "공공 지원사업 / 문화예술 프로그램 및 워크숍 / …" |
| CV — 이름 | 강민경 姜旼炅 |
| CV — 인적사항·학력·경력·어학 | 생년월일, 이메일, Education/Works/Language 연혁 |
| CV — 취미 | 세일링, 서예, 배낭여행 |
| CV — 비하인드 스토리 | "Beyond The CV" 제목 + 점쟁이 이야기 |
| Info 카드 1 — The Mission For You Is | Ongoing 카드 (기간·장소) |
| Info 카드 2 — 사진 찍기, 둥근 마음 쓰기 | Upcoming 워크숍 카드 (해시태그·기간·장소) |
| Info 카드 3 — 미지의 방명록 | Upcoming 워크숍 카드 |
| Info 카드 4 — 배 타고 상경 프로젝트 | Upcoming 개인 프로젝트 카드 |

**팝업 (CONTACT / SUBSCRIBE / 미션)**
| 구역 이름 | 안에 있는 것 |
|---|---|
| 컨택트 팝업 — 이름과 소개 문장 | "집안 어른들은 나를 복덩이라 불렀다…" |
| 컨택트 팝업 — 영상 말풍선 문구 | "미..민경씨?" 등 영상 위 버튼 글자 |
| 구독 팝업 — 안내 문장 | "저의 여정에 새로운 장이…" + 보내는 이메일 |
| 구독 팝업 — 개인정보 안내 | "*입력해주신 이메일 주소는…" |
| 미션 팝업 — 참여 안내 글 | 프로젝트 참여 방법 1~7 |

**노트 (Works 카드의 "Open the NOTE"를 눌렀을 때 나오는 글 — 지금은 대부분 `.` 빈 자리)**
| 구역 이름 |
|---|
| 노트 1 — 대화의 몸집 (케이스 스터디 본문) |
| 노트 2 — TOIL (케이스 스터디 본문) |
| 노트 3 — 보따리 (케이스 스터디 본문) |
| 노트 4 — The Mission For You Is (케이스 스터디 본문) |
| 노트 5 — 어린 밤을 지나고 있어 (케이스 스터디 본문) |
| 노트 6 — 눈을 덮고 푸른 꿈을 꿔 (케이스 스터디 본문) |

**워크숍 상세 (워크숍 카드를 눌렀을 때 — 소개·커리큘럼·역할/기간/장소)**
| 구역 이름 |
|---|
| 워크숍 상세 0 — 끼리끼리 사진 놀이 |
| 워크숍 상세 1 — 책 정리 워크숍 |
| 워크숍 상세 2 — 사진에 깃든 나 |
| 워크숍 상세 3 — 희타, 미지의 여름 이야기 |
| 워크숍 상세 4 — 이희타의 겨울 상상 |
| 워크숍 상세 5 — 영혼의 포토 부스 |

### index.html (첫 화면, 6개)
| 구역 이름 | 안에 있는 것 |
|---|---|
| 랜딩 — 상단 인사말 | "Welcome to MIN'S POSTFOLIO !" 등 |
| 랜딩 — 가운데 이름표와 연도 | "강민경의 포트폴리오 / 2022 - 2026" |
| 랜딩 — 영어 봉투 버튼 문구 | "Which language feels most natural to you?" |
| 랜딩 — 한국어 봉투 버튼 문구 | "어떤 언어가 가장 먼저 눈에 들어오나요?" |
| 랜딩 — 일본어 봉투 버튼 문구 | "どの言葉がいちばんしっくりきますか？" |
| 랜딩 — 하단 연도 | "MAY 2022 - JUL 2026" 등 |

---

## 5. 자주 나는 오류와 해결법

| 증상 | 원인 | 해결 |
|---|---|---|
| 까만 창에 "변경된 내용이 없어요" | 파일을 저장하지 않았다 | 편집기에서 Ctrl+S 하고 다시 더블클릭 |
| push 단계에서 실패 + 영어 에러 | 인터넷 끊김 | 와이파이 확인 후 `업데이트.bat`만 다시 실행 (기록은 이미 남아 있음) |
| push 실패 + 로그인 창이 뜸 | GitHub 로그인 풀림 | 뜨는 창에서 GitHub 로그인 (계정: min-iht) |
| push 실패 + `rejected`라는 단어 | GitHub 쪽에 더 새 내용이 있음 (예: GitHub 사이트에서 직접 수정한 경우) | 아래 "PowerShell 여는 법"대로 창을 열고 `git pull` 입력 → Enter → 끝나면 `업데이트.bat` 다시 실행. 만약 `CONFLICT`라는 단어가 보이면 혼자 풀지 말고 Claude에게 화면을 보여주며 물어보기 |
| commit 실패 + "Please tell me who you are" | 컴퓨터를 바꿔서 git 이름 설정이 없음 | PowerShell을 열고 아래 두 줄을 한 줄씩 입력:<br>`git config --global user.name "강민경(이희타)"`<br>`git config --global user.email "rnk.mov@gmail.com"` |
| 사이트가 안 바뀐 것 같음 | 반영 지연 or 브라우저 캐시 | 1~2분 기다렸다가 Ctrl+F5 |
| 사이트에서 한글이 □□나 이상한 글자로 보임 | 파일이 UTF-8이 아닌 형식으로 저장됨 | VS Code나 Windows 11 메모장은 기본이 UTF-8이라 보통 괜찮다. 다른 편집기를 썼다면 "다른 이름으로 저장 → 인코딩 UTF-8" 후 다시 업데이트 |
| 화면 배치가 깨졌다 / 뭔가 사라졌다 | 태그(`<>`)를 건드렸을 가능성 | 아래 6번으로 되돌리기 |

---

## 6. 망했을 때 되돌리기 (git)

먼저 **PowerShell 여는 법**: Portfolio 폴더 창을 열고, 위 **주소창을 클릭**해서 `powershell`이라고 입력하고 Enter. 파란(또는 까만) 창이 뜬다. 아래 명령을 한 줄씩 입력하고 Enter.

### 상황 A. 고치다 망했는데, 아직 업데이트(bat 실행)는 안 했다
```
git restore .
```
→ 마지막 업데이트 시점으로 **모든 파일**이 되돌아간다. (그 사이 고친 내용은 전부 사라지니, 살리고 싶은 부분이 있으면 먼저 다른 곳에 복사해 두기)

파일 하나만 되돌리려면:
```
git restore "min.postfolio-ko.html"
```

### 상황 B. 이미 업데이트까지 했는데, 방금 올린 걸 취소하고 싶다
```
git revert --no-edit HEAD
git push
```
→ "방금 업데이트를 취소한다"는 새 스냅샷이 만들어져 올라간다. 1~2분 뒤 사이트가 이전 모습으로 돌아온다.

### 상황 C. 한참 전 버전으로 파일을 돌리고 싶다
```
git log --oneline
```
→ 스냅샷 목록이 최신순으로 나온다 (`q`를 누르면 목록에서 빠져나옴). 맨 앞의 7자리 코드(예: `480a423`)를 기억해 두고:
```
git checkout 480a423 -- "min.postfolio-ko.html"
```
→ 그 파일만 그 시점 모습으로 돌아온다. 확인 후 `업데이트.bat`으로 올리면 끝.

### 그래도 모르겠으면
겁먹지 않아도 된다. git에는 모든 버전이 남아 있어서 **진짜로 사라지는 것은 없다.**
이 설명서와 에러 화면을 Claude(또는 개발자)에게 보여주면 몇 분 안에 복구된다.
